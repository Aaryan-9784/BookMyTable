# Increase proxy timeouts for slow MongoDB cold starts
proxy_read_timeout 60s;
proxy_connect_timeout 60s;
proxy_send_timeout 60s;

# Allow larger uploads (matches Express 1mb limit + multer 5mb)
client_max_body_size 10M;
