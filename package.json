{
  "name": "bookmytable",
  "version": "1.0.0",
  "private": true,
  "description": "BookMyTable - Restaurant Reservation Platform",
  "scripts": {
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "client:dev": "npm --prefix client run dev",
    "server:dev": "npm --prefix server run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
